# 0.1.4 validation

This release includes PR #103, accepting known image-config artifact types in index selection and normalizing leading slashes in layer names. Codex reviewed the implementation and an independent Claude pass prompted additional boundary coverage and escaped DEL diagnostics. Tests cover OCI/Docker config artifact types, same-platform attestation exclusion, selected config-type validation, path traversal/limits, and absolute/relative symlink and hardlink resolution. CodeRabbit review was requested but rate-limited and is not counted as an approval.

The reviewed head `003b33204fa10665d35ab1e777dca7527030e9e3` passed all checks in [CI 34434833053](https://github.com/sakajunquality/bunko/actions/runs/34434833053). The branch was subsequently synchronized with the previously reviewed 0.1.3 documentation/default promotion before merge.

Independent public-image verification pinned `ghcr.io/hokaccha/spannerdef@sha256:fa7d23b3869e5d635bc6b63e23be062ab603294a1878a3c703282bcea0a20e6f`. The previous main reproduced the platform-selection error; the corrected implementation extracted `/ko-app/spannerdef` and ran version 1.5.0 on linux/amd64 and linux/arm64 inside nonroot, read-only, network-disabled containers with all capabilities dropped. Contributor-reported application acceptance was not independently repeated.

Release-candidate checks and publication are pending. No publication success is claimed here. External application-machine acceptance and private ECR remain unverified; musl/rebase and setup Action separation/Marketplace publication remain outside this release.

The synchronized PR head `928af55d232dc3150075a2bc1554fb50a292de44` passed [CI 34435348001](https://github.com/sakajunquality/bunko/actions/runs/34435348001) and merged as `db38cf8a16fddbb4e41b37231e91ebef2b9c31d4`.

Preparation pins PATH to signed Bun 1.3.11. Frozen installation, typecheck and all 649 tests passed (7,042 assertions). The initial candidate SHA256 is `6d3b6354336af0e50e5eeef122a04cdb1a3449b107cd0942e4243736b87e6195`; its size is 7,915,576 bytes. The exact candidate passed the disposable application fixture on both Linux architectures: migration, worker/database state, HTTP/native dependencies/file reads and shutdown. Runtime-injection verification is running and will be recorded before publication.

Codex reviewed the release metadata, and Claude found no preparation blockers. A wording clarification was applied to describe Bunko accepting existing index metadata. Both bases and image asset sources use the reviewed `resolveBase` implementation. Published examples and distribution defaults remain at 0.1.3 until 0.1.4 acceptance passes.

Additional bundled-CLI acceptance found that the documented `--asset-cache` was missing from the command allowlist. The release PR adds it to the shared build/resolve/apply options and exercises CLI parsing plus path forwarding for build and resolve. The candidate is being rebuilt and verified again before publication; the initial candidate above will not be published.

After the CLI correction, PATH-pinned Bun 1.3.11 typechecking and all 651 tests passed (7,048 assertions). The publication candidate is 7,915,612 bytes, SHA256 `47bb047de7f6807458cf39f6aea237768d8760bec23ea90b2565a7f8c8a0081b`. CLI regressions explicitly re-enable local caching because the shared test helper disables it by default; this ensures the selected path reaches build validation. Claude reviewed the correction with no blockers. `apply` inherits the same build option set. The rebuilt candidate's public ko image, application and runtime checks are running and will be recorded before tagging.
