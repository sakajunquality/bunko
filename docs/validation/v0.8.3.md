# v0.8.3 validation

This release updates the bundled TypeScript compiler API to 6.0.3 in #187 and includes the npm/pnpm prebuilt application example from #189. TypeScript 7 migration remains tracked in #185.

Candidate and publication validation are in progress. Exact checksums and consumer evidence will be recorded after verification. The independently versioned setup-bunko v0.1.1 Action remains on CLI v0.8.0 by default; its next promotion is deferred. Fixture acceptance does not certify arbitrary applications or hosted-registry deployments.
