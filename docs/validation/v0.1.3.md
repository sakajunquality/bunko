# 0.1.3 validation

This release includes PRs #91, #92, #96, #97, #98, #99 and #100. PRs #91, #92, #96, #97, #98 and #100 are all included in main commit `0778e46b5f9ab10b61703966110415a33aca0d41`, whose CI passed. PR #99 was merged as `a9ca54f36c9e4e2951f4cdd302eda288ad03d869` after all CI checks passed at head `ae4835d63f095464c6da5003b04f19053d1efa90` ([run 34430141688, attempt 2](https://github.com/sakajunquality/bunko/actions/runs/34430141688)). The first attempt hit a 15-second timeout in an existing macOS dependency test; the rerun passed. PR #99 adds image and SHA256-pinned HTTPS asset sources. Review fixes cover partial writes, implied-directory inspection bounds, structural path conflicts, shared multi-platform inputs, offline diagnostics and metadata exclusion. Its final contributor update adds asset-only cache invalidation coverage and clarifies external diagnostics.

Codex reviewed the changes and two Claude passes informed the PR #99 fixes. CodeRabbit review availability and final CI results are recorded in the PR; a rate-limited check is not an approval.

The preparation checkout uses signed Bun 1.3.11. Frozen dependency installation, TypeScript checking and all 642 tests passed (7,013 assertions). The candidate CLI is 7,912,288 bytes with SHA256 `88a1500996dd4b9b11e856d4d982545c1c77ebfa7c5e34cd3f1c14d3c829e192`. The initial candidate passed the disposable application fixture on linux/amd64 and linux/arm64: migration, worker, database state, HTTP/native dependencies/file reads and shutdown.

Local Codex review confirmed the release metadata and notes. Claude's release-text review prompted clearer policy names, analysis limitations and explicit metadata-selection wording. Its conditional merge-order concerns were disproved by Git history: higher-numbered PRs #98 and #100 were merged before #97, and #99 is recorded separately. Release notes describe the artifacts to be published; installation examples and published defaults remain at 0.1.2 until acceptance.

The initial candidate also passed runtime-injection checks on both architectures: signed release/revision verification, static-base rejection, local OCI base inspection, runtime cache reuse and the native-library boundary. Merged #99 main CI [34430609930](https://github.com/sakajunquality/bunko/actions/runs/34430609930) passed all jobs.

CodeRabbit completed #99 review and found one further issue: a disabled asset cache still excluded its configured path. Release PR #101 fixes the guard and adds a regression verifying enabled-cache rejection and disabled-cache success. Claude reviewed that correction; source inspection confirms disabled external caches use only invocation-temporary storage, and path resolution has no cache-write side effects. The hash above identifies the rebuilt candidate after this correction. Repeated application/runtime fixtures, release-PR CI and publication checks are pending and will be recorded in the PR and final evidence before release completion. No publication success is claimed by this preparation record. External application-machine acceptance and private ECR remain unverified; synthetic fixtures do not certify those workloads. musl/rebase and setup Action separation/Marketplace publication remain outside this release.
