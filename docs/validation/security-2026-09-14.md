# Container security refresh validation — 2026-09-14

The CLI container was built locally for linux/arm64 from the pinned Bun 1.4.2 base with the unchanged v0.8.1 CLI payload. Grype 0.118.0 scanned a Docker archive with its current vulnerability database. The downloaded Grype release archive was checked against the upstream checksum manifest. Counts below are package/advisory matches, not unique CVEs or demonstrated exploitable paths.

| Scan result | September 8 snapshot | September 14 snapshot with inherited-package upgrade |
| --- | ---: | ---: |
| Critical | 30 | 8 |
| High | 99 | 62 |
| Medium | 62 | 51 |
| Low | 10 | 7 |
| High/critical with an available fix | 59 | 0 |

The refresh removes 59 fixable high/critical matches. Seventy high/critical matches remain without an available fix in the database and are not claimed resolved. The Security workflow retains the full report for triage and separately gates on fixable high/critical findings; it does not silently exclude unfixed findings from the full report. Database results can change after this checkpoint.

Updated packages include:

| Package | Validated version |
| --- | --- |
| libc6, libc-bin | 2.41-12+deb13u4 |
| libssh2-1t64 | 1.11.1-1+deb13u2 |
| libpcre2-8-0 | 10.46-1~deb13u2 |
| libsqlite3-0 | 3.46.1-7+deb13u2 |
| perl, perl-base, perl-modules-5.40, libperl5.40 | 5.40.1-6+deb13u1 |
| gzip | 1.13-1+deb13u1 |

Debian's tracker confirms the patched glibc version for [CVE-2026-5450](https://security-tracker.debian.org/tracker/CVE-2026-5450) and libssh2 version for [CVE-2026-66033](https://security-tracker.debian.org/tracker/CVE-2026-66033). The base image digest remains pinned; the Dockerfile upgrades its inherited OS packages against the same dated snapshot used for new packages.

Validation completed:

- All six root/example Bun lockfiles passed `bun audit` with no advisories.
- The new workflow passed actionlint 1.7.12; the Dependabot file parsed successfully.
- The refreshed arm64 container built successfully and passed the fixable high/critical Grype gate.
- The existing container smoke passed on arm64: nonroot, read-only builder, no Docker socket, real v2 lockfile, authenticated compile runtime, and execution of the resulting application image.

The local container smoke used a Docker-shared temporary directory; the default macOS temporary directory is not available to this Docker daemon. Native amd64 validation is delegated to the workflow matrix. This change does not rebuild already published release assets or container tags.
