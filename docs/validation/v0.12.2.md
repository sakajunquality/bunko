# v0.12.2 release validation

Release preparation is in progress. Publication and consumer acceptance are not yet complete.

Scope: changes merged in #269 and #270 after v0.12.1, covering failure-output redaction, native libc-variant diagnostics and documentation corrections. The repository package remains private; published CLI, npm and container artifacts are verified separately.

The release uses Bun 1.3.13 for candidate preparation. Version-specific source identities, CLI hashes, CI and publication runs, applicable runtime fixtures and independent consumer results will be recorded after they complete. Previous release results are not evidence for this candidate.

The independently versioned setup-bunko Action is outside this CLI release: immutable v0.2.0 defaults to CLI v0.12.0; users may explicitly select v0.12.2 after publication or opt into `latest`.
