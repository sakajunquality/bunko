# v0.8.2 validation

This release includes security changes #172–#175 and consolidates Dependabot updates #177–#184. TypeScript 7 (#176) is deferred because its package removes JavaScript compiler APIs used at runtime; migration is tracked in #185. TypeScript 6 remains eligible for separate validation.

The Action major upgrades use Node 24 on GitHub-hosted runners. The workflows use no removed Buildx inputs or deprecated build-push environment variables. setup-node continues to use explicit Node 24 with package-manager caching disabled and npm OIDC publication.

## Exact candidate

Prepared with Bun 1.3.13, frozen dependencies, and install scripts disabled. The CLI is 8,002,320 bytes, SHA256 `73d29766acede035226230cb646e70eeacb79b3a4452a5f5a3ada65747646c5d`.

That exact CLI passed eight completed-image structure/behavior scenarios on Linux amd64 and arm64, compiled-image determinism and runtime asset reads, and font rendering in bundle/source modes using both fontconfig and explicit directory discovery. Missing font configuration, missing lazy dependencies, and empty data negative controls failed as expected. Local amd64 runs use emulation; CI image acceptance uses native runners.

Typecheck and the fixed-checkout full suite passed: 879 tests, two platform-specific skips, no failures, and 8,353 assertions. An initial run was invalidated by editing the package version while tests were running. A concurrent-cache test also failed once in that run; it passed the clean full rerun and 15 isolated repetitions. Its initial intermittent failure was not reproduced or explained, so those passes are not a claim of a concurrency fix.

CodeRabbit reviewed the dependency/release diff and identified a stale README release link, which was corrected. The dependency lockfile changes were reviewed locally; CodeRabbit excludes lockfiles.

## Publication and consumers

| Evidence | Result |
| --- | --- |
| Release PR and CI | [#186](https://github.com/sakajunquality/bunko/pull/186), [34849478863](https://github.com/sakajunquality/bunko/actions/runs/34849478863) passed; the original review finding was fixed and resolved; the documentation follow-up was locally reviewed after CodeRabbit reached its review limit |
| Tagged source | `v0.8.2` at `5a52fb58c1c370cbd276eedcee2bb730942034b4` |
| Main CI and signed candidate | [34849978958](https://github.com/sakajunquality/bunko/actions/runs/34849978958), [34849981822](https://github.com/sakajunquality/bunko/actions/runs/34849981822) passed; exact-source provenance verified and CLI bytes match the local candidate |
| Tag CI and GitHub Release | [34850652155](https://github.com/sakajunquality/bunko/actions/runs/34850652155), [34850652061](https://github.com/sakajunquality/bunko/actions/runs/34850652061) passed; all five assets independently downloaded, exact-tag/source provenance verified before CLI execution, checksums/version matched, wrong source ref rejected; public setup installed 0.8.2 |
| GHCR | [34851079124](https://github.com/sakajunquality/bunko/actions/runs/34851079124) passed; index `sha256:5a88d47ee434ae70db4ff8dab40c02ebe8a654931be5b62f67cf8fa2d5dfa401` independently verified against its source identity; anonymous pulls and nonroot/read-only/network-disabled execution passed on amd64/arm64, with both CLI hashes matching GitHub |
| npm preparation | [34851494937](https://github.com/sakajunquality/bunko/actions/runs/34851494937) passed; seven-file allowlist, publish dry-run, install/exec/bunx and spaced-argument checks passed |
| npm publication | [34851712909, attempt 2](https://github.com/sakajunquality/bunko/actions/runs/34851712909) passed; publishing candidate matches preparation byte-for-byte |
| npm integrity | `sha512-e2/f5Py7yChMRd96ZPj+4mq5SJXcXMY5dkSUkYUQHpp/dbZcXWbCKE+bbEI35nDF+Sh9th4E99VvjZ6nHmRwUw==`; independently downloaded tarball matches the successful publishing candidate and its CLI matches GitHub |

CLI tag source, container recipe source and npm packaging source are all `5a52fb58c1c370cbd276eedcee2bb730942034b4`. The first npm publishing attempt failed before publication on a GitHub asset download connection reset; retrying the failed run reused the same source and produced an identical candidate.

Independent consumers verified the npm registry signature and attestation, fresh-cache exact/latest bunx, separate Bun installation, and registry upgrades/rollback 0.8.1 → 0.8.2 → 0.8.1 → 0.8.2. Stable `latest` selects 0.8.2; `next` remains 0.1.0-rc.5. npm metadata initially returned 404 after publication; read-only verification waited for propagation without republishing.

All nine original Dependabot PRs are closed: eight updates were consolidated in #186, and TypeScript 7 was declined with migration tracked in #185. All three Dependabot configuration jobs passed, and a new grouped TypeScript 6 PR (#187) confirms the grouping works. That later compiler update is outside this immutable release and remains under separate review.

The independently versioned setup-bunko v0.1.1 Action retains CLI v0.8.0 by default. Its next promotion is deferred; select `version: v0.8.2` explicitly. Existing immutable releases remain available for rollback. These fixtures do not certify arbitrary applications, OS-library upgrades or additional live hosted-registry behavior.
