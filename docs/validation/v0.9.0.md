# v0.9.0 validation

Release scope: merged SBOM evidence, rebase, Node runtime and keyless work, plus registry credentials in #196, #197 and #199. Native AWS credentials (#198) are excluded pending live AWS acceptance. Existing Docker-compatible AWS helpers remain supported.

## Preparation

The candidate is prepared with Bun 1.3.13, frozen dependencies and installation scripts disabled. Exact candidate hashes, fixture results and publication identities will be recorded after verification. No publication is claimed by this preparation record.

The authentication changes received independently assessed Claude Fable review and CodeRabbit findings were addressed. CodeRabbit's later requests were rate limited; no unavailable review is counted as approval. The AWS-free #199 diff preserves Podman/login behavior and excludes AWS source code, tests and public feature claims. GHCR and Artifact Registry live token-based acceptance is recorded in [registry credential validation](registry-credentials.md); GKE/Cloud Build metadata identity remains unverified.

## Publication

Pending release candidate, tag, GitHub, GHCR and npm acceptance. The independently versioned setup-bunko v0.1.1 remains on CLI v0.8.0 by default; promotion of that repository is deferred.
