# v0.8.1 validation

This patch contains documentation, runnable examples, and automated acceptance coverage from PRs #167–#169. No CLI implementation files changed since v0.8.0. The preceding main CI passed at `da22ba2d76fc83d3dabf0b7d9a13be9c3c8783b0` ([run](https://github.com/sakajunquality/bunko/actions/runs/34809326563)).

Release-candidate and publication evidence will be added after verification. The release keeps existing runtime and rebase limitations; fixture acceptance is not certification of arbitrary OS-library updates or hosted-registry rebase transitions.

The separate setup-bunko v0.1.1 release retains CLI v0.8.0 by default pending an independent promotion; an explicit `version: v0.8.1` selects this CLI.
