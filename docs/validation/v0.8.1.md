# v0.8.1 validation

This patch adds the compile guide/example and completed-image/cross-device acceptance coverage from PRs #167–#169. No CLI implementation files changed since v0.8.0. Existing runtime and rebase limitations remain; fixture acceptance does not certify arbitrary applications, OS-library upgrades, or hosted-registry rebase transitions.

## Exact candidate

Preparation used Bun 1.3.13 with frozen dependencies and install scripts disabled. The CLI is 8,000,474 bytes, SHA256 `1073801e537c1dcfa06a3d8fb24ed1dc71eff0eafc2e65dce45047d6e16b9718`. Typecheck and 869 tests passed, with two platform-specific skips, no failures, and 8,289 assertions.

The exact candidate passed all eight completed-image behavioral scenarios across Linux amd64/arm64: working bundle images, source-mode module-relative catalog access, and expected failures for missing data and lazy dependencies. Structure checks and the loaded image configuration were verified independently of startup success. The same CLI passed both-platform compiled-image determinism, runtime asset reads, and authenticated Bun revision checks. Local amd64 execution used emulation on an arm64 host; CI exercised native Linux runners, including cross-device cache operations.

The release diff received local review. CodeRabbit was requested on the release PR but rate limited; no external approval is claimed for that PR.

## Publication and consumers

| Evidence | Result |
| --- | --- |
| Release PR and CI | [#170](https://github.com/sakajunquality/bunko/pull/170), [34811682483](https://github.com/sakajunquality/bunko/actions/runs/34811682483) passed |
| Tagged source | `v0.8.1` at `46accad26dffa92ab039031a3301ef24ea9f80ff` |
| Main CI and signed candidate | [34812146676](https://github.com/sakajunquality/bunko/actions/runs/34812146676), [34812148352](https://github.com/sakajunquality/bunko/actions/runs/34812148352) passed; exact-source provenance verified and CLI bytes match the local candidate |
| Tag CI and GitHub Release | [34812525644](https://github.com/sakajunquality/bunko/actions/runs/34812525644), [34812525693](https://github.com/sakajunquality/bunko/actions/runs/34812525693) passed; all five assets independently downloaded, exact-tag/source provenance verified before CLI execution, checksums/version matched, wrong source ref rejected; public setup installed 0.8.1 |
| GHCR | [34812832551](https://github.com/sakajunquality/bunko/actions/runs/34812832551) passed; index `sha256:96a39d371765f758f67db34ffdaeb97b9138d928c0c856b82ca51161086fefc6` independently verified against its source identity; anonymous pulls and nonroot/read-only/network-disabled execution passed on amd64/arm64, with both CLI hashes matching GitHub |
| npm preparation and publication | [34812867251](https://github.com/sakajunquality/bunko/actions/runs/34812867251), [34812990210](https://github.com/sakajunquality/bunko/actions/runs/34812990210) passed; seven-file allowlist, publish dry-run, install/exec/bunx and spaced-argument checks passed; candidates are byte-identical |
| npm integrity | `sha512-BBzJf4s0ywfspWB08z+TnB2LuKw/bcUdvfxNz/sv5fx0kgskxTFslZ1YYPq+wfH3+E1RESZyTWCjzQQwEzcLJQ==`; the independently downloaded registry tarball matches the successful publishing candidate, and its CLI matches GitHub |

CLI tag source, container recipe source and npm packaging source are all `46accad26dffa92ab039031a3301ef24ea9f80ff`. Registry metadata initially returned 404 after npm accepted publication; verification waited for propagation without republishing.

Independent consumers verified the npm registry signature and attestation, fresh-cache exact/latest bunx, separate Bun installation, and registry upgrades/rollback 0.8.0 → 0.8.1 → 0.8.0 → 0.8.1. Stable `latest` selects 0.8.1; `next` remains 0.1.0-rc.5. Publication used GitHub Actions trusted publishing, without a local publish or long-lived npm token.

The independently versioned setup-bunko v0.1.1 Action retains CLI v0.8.0 by default. Its next promotion is deferred; select `version: v0.8.1` explicitly. Existing immutable releases remain available for rollback.
