# v0.12.4 validation

Release preparation is in progress; publication and independent consumer acceptance are not yet claimed.

Scope: #283 fixes #278–#280. Missing prepared-base blobs gain actionable diagnostics, kind defaults to the host architecture, and kubectl preflight preserves bounded redacted CLI diagnostics and report exit status without publishing on failure. Explicit platform precedence and original base index identity are retained.

Host Bun support and persisted formats remain unchanged. The kind default change is documented under Breaking changes in the release notes. The separate OCI audit's four implementation findings remain unresolved. The independently versioned setup-bunko v0.2.0 retains default CLI v0.12.0.

Publication identities and consumer evidence will be added after artifact acceptance.
