# Bun 1.4 lock fixture

Generated by official Bun 1.4.2 (`1.4.2+744846f84`) using `bun install --ignore-scripts --lockfile-only`. Preserve the generated schema and package tuple. The container smoke test separately generates and frozen-installs a fresh lock with Bun 1.4.2.
